---
name: 던전
description: The Last Theater — 슬롯 로그라이크 프로젝트를 이어서 작업한다. 확정된 설계와 현재 진행 상태를 파악하고 다음 작업 지점을 제시한다. 사용자가 /던전 을 호출하거나 이 게임 프로젝트를 이어서 하겠다고 할 때 사용한다.
---

# The Last Theater — 작업 이어가기

## 먼저 할 일

1. **[SESSION_LOGS/sessions/](../../../SESSION_LOGS/sessions/) 의 최신 파일을 읽는다.** 최근 세션에서 무엇을 정했고 다음이 무엇인지 여기 있다. 대화 기록은 PC 사이를 넘어가지 않는다 — 이 폴더가 그 역할을 한다
2. **[DESIGN.md](../../../DESIGN.md) 를 읽는다.** 이게 유일한 진실 공급원이다. **50장까지 확정**되어 있다
3. **23장 "폐기한 안과 그 이유" 를 반드시 확인한다.** 이미 검토하고 버린 안이 15개 넘게 기록되어 있다. 같은 논의를 반복하지 말 것
4. **22장 TBD** 에서 다음 작업 지점을 고른다
5. 아트 작업이면 [ART_PROMPTS.md](../../../ART_PROMPTS.md) 를, 애니메이션이면 DESIGN.md 21장과 [enemy_animation/WORKLOG.md](../../../enemy_animation/WORKLOG.md) 를 읽는다

## 이 프로젝트가 뭔지 한 줄로

3릴 슬롯을 돌려 **장면을 연출하고**, 상점에서 **릴 내부 심볼 구성 자체를 사고팔며** 성장하는 다크 판타지 인형극 로그라이크. PC 세로 화면. Unity 6000.2.1f1 / C#.

**설계 중심축:** 릴 = 눈에 보이는 덱. 스핀 = 드로우. 상점 = 덱빌딩. **유물 = 빌드의 정체성.**

**핵심 판타지:** *"나는 싸우는 게 아니라 죽음을 연출한다."*

> **서커스 테마는 폐기했다**(23장). 이미 만든 아트는 그대로 쓴다 — 광대는 역할 심볼, 나무 인형은 인형극 그 자체, 곰은 배역에 갇힌 배우다.

## 절대 흔들지 말 것

이 결정들은 여러 차례 검토를 거쳐 확정됐다. 바꾸려면 근거가 있어야 한다.

- **기본 처리는 항상 무료로 존재한다.** 기술 0개로도 게임이 굴러가야 한다. 그래서 기술을 봉인·처벌하는 보스가 성립한다
- **자원은 🎟 회전권 하나.** 앙코르·기술 승급·넛지가 모두 이 하나를 두고 경쟁한다
- **매 턴 반드시 뭔가 일어난다.** 헛턴 없음. 1줄은 무조건 돌아간다
- **랜덤은 계획 가능해야 한다.** 적 릴은 한 턴 미리 공개하고(48장), 미리보기 칸을 보여주고, 교정 수단(넛지)을 판다
- **어려운 조합일수록 강해야 한다**(49장). 3종 조합이 1종보다 21배 어려운데 코스트당 값이 같았던 적이 있다. 일반 덱을 깎지 말고 **어려운 쪽을 올려서** 맞춘다
- **UI는 명도로 분리하고 채도로 분리하지 않는다.** 채도를 올리면 즉시 모바일 카지노 앱처럼 보인다
- **idle은 코드 애니메이션, 공격은 프레임 시트.** 이유는 DESIGN.md 21.1

## 현재 진행 상태

| 항목 | 상태 |
|---|---|
| 규칙 설계 | **완료** — DESIGN.md 50장 |
| **수치 설계** | **완료** — 시뮬레이터 실측으로 확정 (27·28·41·42장) |
| 화면 레이아웃 | **완료** — 5밴드, 1920×1080 실측 검증 (20장) |
| 애니메이션 방침 | **완료** (21장) |
| 캐릭터 8종 | **완료** — 승률 스프레드 19pp (50.5) |
| 1·2막 | **완료** (11장) |
| **3막** | **미착수** ← 가장 큰 남은 덩어리 (적 4종 + 보스 초대 감독) |
| 아트 레퍼런스 | 배경 판·HUD 판 확정. 캐릭터 스프라이트 진행 중 |
| **Unity 프로젝트** | **미착수** — C# 코드는 한 줄도 없다 |

규칙은 웹 시뮬레이터로 먼저 검증하고 Unity로 옮기는 순서다. 지금 「구현」은 전부 `tools/` 의 JS다.

## 도구

브라우저에서 파일을 직접 열면 된다. 빌드 과정 없음.

- **[tools/play.html](../../../tools/play.html)** — 플레이 시뮬레이터. 실제로 한 판을 사람이 둔다. 규칙 변경의 감각을 확인하는 곳
- **[tools/agents.html](../../../tools/agents.html)** — 100명(또는 그 이상) 봇 시뮬레이터. **밸런스 주장은 여기 숫자로 한다.** 승률·스프레드·상연 티어가 여기서 나온다
- **[tools/engine.js](../../../tools/engine.js)** — 규칙 엔진. play·agents가 같은 파일을 공유한다. 어떤 규칙이 실제로 로드됐는지 화면에 표시된다(캐시가 낡으면 바로 보이라고)
- **[tools/sim.js](../../../tools/sim.js)** — 봇 정책(POLICIES). 봇은 매 턴 손패와 코스트로 가능한 상연 순서를 실제로 탐색한다. **한 턴만 본다**는 한계가 있어 커튼콜 같은 축은 실제 사람보다 낮게 측정된다(50.4)
- **[wireframe/combat.html](../../../wireframe/combat.html)** — 전투 화면 그레이박스. 열면 밴드·요소 크기가 실시간 실측으로 표시되고 기준 위반이 판정된다
- **[tools/spritesheet.html](../../../tools/spritesheet.html)** — 스프라이트 시트 검사·정렬·내보내기. 시트를 붙여넣으면 캐릭터를 자동 감지해 자르고, 정렬 편차와 프레임 변화율을 판정하고, 균등 셀 PNG·낱장·GIF로 내보낸다
- **reference/demo12/** — 옛 구현본. 참고용이지 현재 규칙이 아니다
- **enemy_animation/** — 적 애니메이션 원본과 작업 규칙 (사용자가 관리)

## 작업 방식

이 사용자와 일할 때의 규칙이다.

- **설계를 먼저 대화로 다듬는다.** "만들자"는 말이 나와도 곧바로 파일을 쓰지 말고, 트레이드오프와 추천안을 제시하고 합의를 받는다. 단순 나열이 아니라 **함정을 지적하고 대안과 이유를 함께** 주는 것을 선호한다
- **합의된 규칙은 즉시 문서로 고정한다.** 대화로 들고 있지 않는다
- **밸런스는 의견이 아니라 측정으로 정한다.** agents.html 로 돌려서 숫자를 보고 말한다. "약한 것 같다"가 아니라 "승률 24%, 스프레드 19pp"로 말한다
- **레이아웃·수치는 눈이 아니라 측정으로 검증한다.** 그레이박스를 실제 해상도로 열어 숫자를 읽는다. 실측이 SPIN 버튼 폭 버그처럼 눈으로 못 잡는 문제를 잡아냈다
- **커밋 메시지는 한국어가 아니라 영어 산문으로**, 무엇을·왜 바꿨는지 쓴다. 큰따옴표를 넣으면 PowerShell here-string 파싱이 깨지니 피하거나 `git commit -F` 를 쓴다
- **`git add -A` 를 쓰지 않는다.** 파일을 명시한다. 한 번 원치 않는 이미지가 히스토리에 들어갔다
- 커밋은 사용자가 요청할 때 한다
- **세션 끝에 세션 기록을 남긴다.** 다른 PC에서 이어받는 유일한 통로다 — 아래 참조

## 여러 PC에서 이어서 작업하기

대화 기록은 이 PC의 로컬 세션에만 있다. 기기를 넘어가는 것은 **저장소 파일뿐이다.**

```powershell
# 이어받기
git pull   →   /던전

# 세션이 끝나면 — 기록을 만들고 채운 뒤 커밋·push
.\scripts\new-session-log.ps1 -Title "전투 UI 수정"
.\scripts\publish-session.ps1 -Message "docs: record combat UI session"
```

그래서 결정을 대화에 남기지 않고 문서에 적는다. **확정된 규칙은 `DESIGN.md` 에 장으로, 무엇을 왜 정했는지는 `SESSION_LOGS/sessions/` 에.** 이미지는 `SESSION_LOGS/images/` 에 넣고 상대 경로로 건다.

**대화 원문(jsonl)은 커밋하지 않는다.** 시행착오와 폐기된 안이 섞여 있어 새 세션이 확정된 것과 버린 것을 구분하지 못한다 — 23장이 폐기안을 따로 기록하는 이유와 같다.

## 아트 요청 시 주의

- 스프라이트 배경은 **마젠타 `#FF00FF`** 로 요청한다. 어두운 자주 배경은 광대 옷 그림자와 색이 가까워 투명화할 때 외곽이 깎인다
- **idle 시트를 요구하지 않는다.** 프레임마다 새로 그려져서 반복 재생 시 픽셀이 끓는다. idle은 정지 스프라이트 한 장만 받는다
- 공격 시트는 **골반 피벗 고정**, **이펙트 미포함**을 명시한다
- 레퍼런스는 **배경 판과 UI 판을 따로** 뽑는다. 한 장에 다 담으려 하면 분위기와 게임성 중 하나가 죽는다

## 다음 작업 후보

DESIGN.md 22장 TBD 에서 뽑았다.

- **3막 전체** — 적 4종 + 보스 「초대 감독」. 구현본과 옛 기획서 모두 2막까지만 있다. **가장 큰 덩어리**
- **철갑 인형 배치**(11.2) — 관통 축이 승천 4에서 탈락한다. 방어력 15~20 인 적이 없으면 관통은 열등한 단일딜일 뿐이다. 밸런스 문제 중 원인이 가장 분명한 항목
- **초기 릴 확정** — 아키타입 체계로 바뀌어 재검토가 필요하다. 초보가 한 계열에 4장쯤 몰려 시작하는 형태가 후보
- **넛지 유물** — 8.1에서 넛지를 유물 전용으로 정했는데 정작 주는 유물이 없다
- **어릿광대 캐릭터**(19.3) — 어느 덱에서도 최적이 아니다. 보석 1장 대 최대 HP −5 의 교환이 성립하지 않는다
- **Unity 그레이박스** — 20장 밴드 명세를 실제 Unity 씬으로. C# 코드가 처음 들어가는 지점
- **광대 idle 프레임 검수** — enemy_animation/WORKLOG.md 의 "다음 작업"
