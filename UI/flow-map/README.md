# UI 흐름도 조립본

- `preview.png`: Figma `00 FLOW MAP` 가져오기용 6144×4096 대형 보드.
- `preview-1920.png`: 대화와 빠른 검토용 1920×1280 축소본.
- 새 화면을 그리지 않고 `UI/`에 있는 기존 확정 시안만 축소 배치했다.
- 전체 이동은 금색 실선, 지도 복귀는 적색 공용선, 오버레이는 별도 하단 구역으로 구분한다.
- 상세 버튼 목적지와 Figma 동작은 `UI/FLOW_MAP.md`를 기준으로 한다.

## 다음 단계

이 조립본이 확정되면 Figma의 `01 FULL SCREENS`에 원본 1920×1080 이미지를 등록하고, `03 PROTOTYPE`에서 이미지 속 실제 버튼 위치에 투명 핫스팟을 얹는다.

